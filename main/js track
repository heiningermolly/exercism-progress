
// ongoing https://exercism.org/tracks/javascript/exercises/annalyns-infiltration/edit


//finished:

//Annalyn's Infiltration
//things learned: don't be redundant with booleans. If a parameter is a boolean, don't "= true" with it in the code. Just list it or !it.

// - Lucian's Luscious Lasagna
const PREPARATION_MINUTES_PER_LAYER = 2;

export const EXPECTED_MINUTES_IN_OVEN = 40;
export function remainingMinutesInOven(actualMinutesInOven) {
  return EXPECTED_MINUTES_IN_OVEN  - actualMinutesInOven;
}
export function preparationTimeInMinutes(numberOfLayers) {
  return numberOfLayers * PREPARATION_MINUTES_PER_LAYER;
}
export function totalTimeInMinutes(numberOfLayers, actualMinutesInOven) {
  return preparationTimeInMinutes(numberOfLayers) + actualMinutesInOven;
}
